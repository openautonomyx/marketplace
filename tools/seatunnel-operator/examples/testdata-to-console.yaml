# GENERATED by testdata/generate_test_data.py -- do not edit by hand.
# A SeaTunnelJob that streams 8 known test records through SeaTunnel and
# prints them to the console. Apply it and watch the operator reconcile it:
#   kubectl apply -f testdata-to-console.yaml
#   kubectl get seatunneljobs -n seatunnel
#   kubectl logs -n seatunnel job/$(kubectl get stj testdata-to-console -n seatunnel -o jsonpath='{.status.jobName}')
apiVersion: marketplace.openautonomyx.io/v1alpha1
kind: SeaTunnelJob
metadata:
  name: testdata-to-console
  namespace: seatunnel
spec:
  image: apache/seatunnel:2.3.11
  engine: local
  config: |
    env {
      parallelism = 1
      job.mode = "BATCH"
    }
    source {
      FakeSource {
        schema = { fields { id = bigint, name = string, age = int, country = string } }
        rows = [
        { kind = INSERT, fields = [1, "Jordan Nguyen", 65, "DE"] }
        { kind = INSERT, fields = [2, "Morgan Smith", 26, "GB"] }
        { kind = INSERT, fields = [3, "Sasha Patel", 55, "JP"] }
        { kind = INSERT, fields = [4, "Avery Nguyen", 23, "BR"] }
        { kind = INSERT, fields = [5, "Morgan Mori", 56, "US"] }
        { kind = INSERT, fields = [6, "Sasha Smith", 63, "JP"] }
        { kind = INSERT, fields = [7, "Morgan Larsson", 55, "DE"] }
        { kind = INSERT, fields = [8, "Avery Garcia", 62, "JP"] }
        ]
      }
    }
    sink {
      Console {}
    }
