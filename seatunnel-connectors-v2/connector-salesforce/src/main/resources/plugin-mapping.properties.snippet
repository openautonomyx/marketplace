# Append these two lines to the repository-root plugin-mapping.properties so the
# engine can resolve the "Salesforce" identifier to this connector module.
seatunnel.source.Salesforce = connector-salesforce
seatunnel.sink.Salesforce = connector-salesforce
