{
  "name": "@openautonomyx/marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "Certified skill marketplace with evaluation, benchmarking, certification, registry, signature, and trust engines.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
